import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.gracefulcapybaratwirl',
  appName: 'graceful-capybara-twirl',
  webDir: 'dist'
};

export default config;
